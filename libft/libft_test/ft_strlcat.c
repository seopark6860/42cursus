#include <string.h>

size_t	ft_len(char *str)
{
	int i;
	i = 0;
	while (str[i])
		i++;
	return (i);
}
size_t	strlcat(char *dst, const char *src, size_t dstsize)
{
	unsigned ing i;
   	unsigned int s_len;
	unsigned int d_len;
	unsigned int res;

	d_len = ft_len(dst);
	s_len = ft_len(src);
	if (
	

}
int main()
{

}
